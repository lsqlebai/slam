server:
  ip: 0.0.0.0
  port: 3000

db:
  path: /data/sport.db

ai:
  key: ""
  job_dir: "/data/ai-jobs"
  worker_concurrency: 1
  max_attempts: 3
  retry_delays_seconds: [15, 60]

security:
  salt: "slam-server-salt"
  key: "change-me-key"
  jwt_ttl_seconds: 2592000
