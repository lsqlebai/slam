AI_API_KEY=

# Replace this example with the proxy's LAN address and port. The proxy must
# accept connections from Docker (some proxy apps call this "Allow LAN").
BUILD_HTTPS_PROXY=http://192.168.1.100:7890
